# Five-step optimizer — 2026-07-27

Hand-written novel-command and skill densify. Survives reprint only if re-applied or folded into Printing Press novel scaffolds.

## Changes

1. **SKILL.md densify (v1.3.0)** — Dropped Discovery Signals, sports-domain learning examples, and restated platform prose. Kept install, family-safety wind-signal hard rule, agent envelope, paths, exit codes.
2. **Parallel multi-page fetch** — `internal/cli/fetch_pages.go`; used by `digest`, `storm`, `watch`, `approach`.
3. **`watch` HTML signal match** — `signal_match.go`; exposes `signal`, `signal_matched`, `wind_signals`, `bulletin_pdfs`. Stale "PDF-only" Long text removed.
4. **`approach` bulletin coord fallback** — When synopsis lacks coordinates, use severe-weather-bulletin location panel.

## Do not delete

- Full `learn` / `store` / platform packages (Printing Press contract).
- Raw link-scrape commands (`weather`, `climate`, `tropical-cyclone`).
