# AWS station observations (issue #17) — 20260803

Hand-written novel surface for PAGASA Automated Weather Stations. Survives reprint only if re-applied or folded into Printing Press novel scaffolds.

## Changes

1. **`internal/pagasa/aws.go`** — `ParseAWSStations` + `ParseAWSLastUpdated` with fixed PHT zone (no tzdata).
2. **`internal/pagasa/testdata/aws_stations.html`** — fixture from bagong table.
3. **`internal/store/extras.go`** — additive `aws_obs` table + indexes; **do not bump** `StoreSchemaVersion`.
4. **`internal/store/aws_obs.go`** — upsert (ON CONFLICT DO UPDATE), list, 14d prune on capture.
5. **`internal/cli/obs.go`** — registered via `registerNovelCommand`: live `obs`, `obs --capture`, `obs history`.
6. **`internal/cli/which.go`** — index entries for station/AWS (generated file — re-seed after reprint).
7. **README.md / SKILL.md** — AWS source, capture cadence, grain vs synopsis `history`.

## Source

- Host: `https://bagong.pagasa.dost.gov.ph/automated-weather-station/`
- Not `www` BaseURL default; bagong client is one-shot in `obs` only.

## Do not

- Overload `pagasa_snapshot` / synopsis `history` for station series.
- Mark capture as `mcp:local-write` alone (open-world HTTP).
- Enable hermes0 capture timer in this patch.
- Bump store schema version for this table.
